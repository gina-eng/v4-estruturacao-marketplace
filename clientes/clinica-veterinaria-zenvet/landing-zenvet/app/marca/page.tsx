import SlideDeck from "./SlideDeck";

export const metadata = {
  title: "Manual de Marca · Zenvet",
  robots: { index: false, follow: false },
};

export default function MarcaPage() {
  return <SlideDeck />;
}
